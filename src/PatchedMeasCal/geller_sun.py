def calculate_correlators(backend, provider=None):
    # get n_qubits
    

def correlator_A(results):
    for i in results:
        for j in i:
            ptrace(results[i]) - ptrace(n_res[j])

def correlator_B(results):

